@{
    Layout = "_Layout";
}

@model SensitiveInfo
@{
    ViewData["Title"] = "View Sensitive Information";
}

<h2>@ViewData["Title"]</h2>

<div class="card">
    <div class="card-body">
        <h5 class="card-title">Sensitive Information #@Model.Id</h5>
        <p class="card-text">@Model.Info</p>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Dashboard" class="btn btn-primary">Back to Dashboard</a>
</div>

<div class="mt-4">
    <div class="alert alert-info">
        <h4 class="alert-heading">Security Note</h4>
        <p>
            This page demonstrates the potential for Insecure Direct Object Reference (IDOR) vulnerabilities.
        </p>
        <p>
            In insecure mode, you can access this information by simply changing the ID in the URL, even if it belongs to another user.
            In secure mode, proper access controls prevent unauthorized access to other users' sensitive information.
        </p>
    </div>
</div>